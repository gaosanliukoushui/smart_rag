# SmartRAG Configuration

# Application
APP_NAME=SmartRAG
DEBUG=true
API_V1_PREFIX=/api/v1

# JWT Authentication
JWT_SECRET_KEY=your-secret-key-here  # Generate with: python -c "import secrets; print(secrets.token_hex(32))"
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/smartrag

# Redis
REDIS_URL=redis://localhost:6379/0

# Vector Database
VECTOR_DB_TYPE=chroma
CHROMA_PERSIST_DIR=./data/chroma
RETRIEVAL_MODE=vector
BM25_TOKENIZER=char_ngram
RAG_EVAL_DATASET=evals/qa_set.jsonl

# Agent planner
# rule: deterministic local planner
# llm_fallback: use LLM planner and fall back to rule planner on errors
# llm: require LLM planner and fail if planning is invalid
AGENT_PLANNER_MODE=rule
AGENT_PLANNER_MODEL=
AGENT_PLANNER_MAX_STEPS=8

# LLM Provider (deepseek / qwen / openai / ollama)
LLM_PROVIDER=deepseek

# DeepSeek
DEEPSEEK_API_KEY=your-deepseek-api-key
DEEPSEEK_MODEL=deepseek-chat
DEEPSEEK_BASE_URL=https://api.deepseek.com

# Qwen (Alibaba Cloud)
QWEN_API_KEY=your-qwen-api-key
QWEN_MODEL=qwen-plus
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# OpenAI
OPENAI_API_KEY=your-openai-api-key
OPENAI_MODEL=gpt-4o

# Ollama (Local)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2
OLLAMA_NUM_CTX=4096
OLLAMA_TEMPERATURE=0.7
OLLAMA_TIMEOUT=120

# Embedding
EMBEDDING_MODEL=BAAI/bge-m3
EMBEDDING_DEVICE=cpu
EMBEDDING_DIM=1024

# Reranker
RERANKER_MODEL=BAAI/bge-reranker-v2-m3

# Upload
UPLOAD_DIR=./data/uploads
MAX_UPLOAD_SIZE=104857600

# Chunk Settings
CHUNK_SIZE=500
CHUNK_OVERLAP=100

# Environment
ENVIRONMENT=development
LOG_LEVEL=INFO
LOG_FORMAT=console
